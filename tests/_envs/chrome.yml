modules:
  config:
      WebDriver:
          browser: 'chrome'
          capabilities:
              chromeOptions:
                  args:
                    - 'load-extension=tests/_envs/chrome/AdBlock'
