modules:
  enabled:
      - \Helper\ChromeHelper
  config:
      \Helper\ChromeHelper:
          browser: 'chrome'
          url: 'http://www.detectadblock.com/'
          capabilities:
              chromeOptions:
                  args:
                    - 'load-extension=tests/_data/adBlock'
